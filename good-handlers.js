module.exports = {
    'POST /analytics' : function(userData) {
        // store what we want somewhere
        // with timestamp
        // and google/github etc
    },

    'GET /analytics' : function(request, reply) {
        // read analytics from db
    }
};
